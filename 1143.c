#include <stdio.h>

int main() {
    int vet[10], num;

    for (int i = 0; i < 10; i++)
    {
        scanf("%d", %num);
        if (num <=0)
        {
            vet[i] = 1;
        }
        else
        {
            vet[i] = num;
        }
        
    }

}